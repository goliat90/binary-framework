#ifndef VERBOSITY_H
#define VERBOSITY_H
/*  This is a header that defines levels of printout for the system. */

enum verboseLevel{
    SILENT = 0,
    MINIMAL = 1,
    DETAILED = 2,
    DEBUGGING = 3
};

#endif
